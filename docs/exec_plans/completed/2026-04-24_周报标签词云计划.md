# 计划：2026-04-24_周报标签词云

## 目的 / 大图景
- 在周报“今日概览”中增加一幅基于本次纳入文章总结标签生成的词云图，用更直观的方式展示本周研究对象与主题分布。
- 词云图作为周报产物的一部分落到输出目录，并通过 Obsidian embed 写入周报正文。

## 背景与定位
- 当前“今日概览”只有表格和文字总结，缺少直观图形。
- 用户要求把词云图放在“### 本周重点方向分布”前。
- 本轮只处理周报标签词云，不调整 UI，不压缩现有中文概括。

## 工作范围
- `config/templates/daily_report_template.md`
- `src/sciencemonitor/reporting.py`
- `src/sciencemonitor/reporting_template.py`
- `tests/test_reporting.py`
- 必要时更新 `evals/golden/daily_report.md`

## 非目标
- 不调整单篇总结标签本身
- 不改周报其他板块的文案策略
- 不改 UI 展示

## 进度
- [x] 步骤 1：确定词云资源输出路径和模板占位位置
- [x] 步骤 2：实现词云图片生成与周报 embed 渲染，并补测试
- [x] 步骤 3：重建 2026-04-17 周报，更新 golden / harness 校验并归档计划

## 计划中的工作
- 先确定周报图片应写到哪里，以及 Markdown 中如何稳定引用。
- 再实现按标签频次生成词云图片的逻辑，并在默认模板中插入。
- 然后用真实周报和 golden 夹具一起验证，保证后续生成稳定。

## 具体步骤
1. 在默认模板中添加词云占位，位置放在“本周重点方向分布”前。
2. 在周报生成链路中统计标签频次并输出一张图片到周报资源目录。
3. 通过 Obsidian embed 把图片插入 Markdown。
4. 补测试，覆盖默认模板渲染和输出顺序。
5. 重建 2026-04-17 周报并运行 `golden-eval`、`harness-check`。

## 发现与意外
- 待执行中补充。

## 决策记录
- 优先使用本地程序生成词云，不引入新的 LLM 调用。

## 结果与复盘
- 默认周报模板已在“今日概览”下新增标签词云图片，并通过 Obsidian embed 引用本次周报对应的 PNG 资源。
- 真实 2026-04-17 周报已生成词云图片并完成位置调整。
- golden 与 harness 已同步。

## 验证
- `./.venv/bin/python -m pytest -q`
- `./scripts/run_science_monitor.sh golden-eval`
- `./scripts/run_science_monitor.sh harness-check`
