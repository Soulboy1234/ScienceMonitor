# 计划：2026-04-28_版本号语义修正

## 目的 / 大图景
- 修正 `v1.5.0` 版本号语义错误，按用户明确的 `x.y.z` 规则重新记录为 `v2.0.0`。
- 将版本规则写入长期文档，避免后续“大小版本”理解漂移。
- 补充本轮版本范围说明：不仅包含 Ollama 接口，也包含周报链路和周报输出风格优化。

## 背景与定位
- 用户明确规则：大版本是 `x`，中版本是 `y`，小版本是 `z`。
- 上一轮我把“大版本更新”错误记录为 `v1.5.0`，这实际是中版本更新。
- `v1.5.0` tag 目前只在本地存在，远端没有 `v1.5.0` 或 `v2.0.0` tag。

## 工作范围
- `CHANGELOG.md`
- `src/sciencemonitor/__init__.py`
- `docs/exec_plans/GovernanceBoard.md`
- 版本规则相关文档
- Git tag：删除本地错误 `v1.5.0`，创建正确 `v2.0.0`

## 非目标
- 不改动业务功能代码。
- 不重跑真实周报或 LLM 任务。
- 不推送 GitHub，除非用户后续要求。

## 进度
- [x] 步骤 1：补充版本号规则文档
- [x] 步骤 2：把本轮版本记录从 `v1.5.0` 修正为 `v2.0.0`
- [x] 步骤 3：修正 tag、验证并归档计划

## 计划中的工作
- 先在版本记录文档中写清 `x.y.z` 语义。
- 再批量替换当前错误版本号和治理看板目标。
- 删除本地错误 tag，重新创建 `v2.0.0` tag。
- 运行轻量验证和 harness-check。

## 具体步骤
1. 更新 CHANGELOG 的版本规则说明。
2. 将 `__version__`、CHANGELOG、GovernanceBoard 从 `1.5.0` 改为 `2.0.0`。
3. 在 CHANGELOG 的 `v2.0.0` 主要变化中补充“周报优化”。
4. 删除本地 `v1.5.0` tag，创建本地 `v2.0.0` tag。
5. 运行 `git diff --check` 和 `harness-check`。

## 发现与意外
- 错误的 `v1.5.0` tag 仅存在于本地，远端尚未发布，修正成本较低。
- 版本修正不需要修改业务代码；只涉及版本常量、版本记录、治理看板和历史计划记录。

## 决策记录
- 本轮版本号按用户规则应为 `v2.0.0`，不是 `v1.5.0`。
- 删除本地错误 tag `v1.5.0`，并在修正后的 release commit 上创建本地 tag `v2.0.0`。

## 结果与复盘
- 已将版本号规则写入 `CHANGELOG.md`：`x` 为大版本，`y` 为中版本，`z` 为小版本。
- 已将当前版本记录、包版本常量和 GovernanceBoard 从 `v1.5.0` 修正为 `v2.0.0`。
- 已补充本轮版本范围：除 Ollama 接口外，也包含周报输出与推荐链路优化。
- 已删除本地错误 tag `v1.5.0`，并创建本地正确 tag `v2.0.0`。

## 验证
- `./.venv/bin/python -m pytest tests/test_entropy.py tests/test_config_ui.py -q`：24 passed
- `git diff --check`：通过
- `./scripts/run_science_monitor.sh harness-check`：overall=ok
