# 计划：2026-04-24_周报词云图优化

## 目的 / 大图景
- 优化周报词云图的可读性和资产治理：图内增加时间范围说明，保留所有非管理标签，并自动清理未被任何周报引用的旧词云图。

## 背景与定位
- 当前词云图已接入周报，但还缺少时间范围标注。
- 当前标签筛选没有显式排除管理标签，也没有保证所有业务标签都进入词云。
- `research_reports/assets` 会积累旧词云图，需要按实际引用关系做清理。

## 工作范围
- `src/sciencemonitor/reporting.py`
- `tests/test_reporting.py`
- 真实周报输出与 `research_reports/assets/`

## 非目标
- 不调整词云图在周报中的位置
- 不改周报其他区块文案
- 不改 UI

## 进度
- [x] 步骤 1：梳理当前词云统计、输出和资产清理边界
- [x] 步骤 2：实现时间范围标注、标签筛选优化和未引用词云清理，并补测试
- [x] 步骤 3：重建 2026-04-17 周报并运行 golden / harness 校验，归档计划

## 计划中的工作
- 先检查现有词云统计函数和输出目录结构。
- 再实现非管理标签全量纳入、图内时间范围标注和资产清理逻辑。
- 最后用真实周报与测试双重验证，确保输出稳定。

## 具体步骤
1. 核对当前词云图统计和资源写入位置。
2. 增加管理标签过滤规则，并保留其他全部标签。
3. 在词云图左上角绘制本次周报时间范围。
4. 根据所有周报 Markdown 的实际 embed 清理未引用的 `* 标签词云.png`。
5. 补测试，覆盖标签筛选、图片写出和旧资源清理。
6. 重建 2026-04-17 周报，运行 `golden-eval` 与 `harness-check`。

## 发现与意外
- 待执行中补充。

## 决策记录
- 词云图仍保持本地确定性生成，不引入新的 LLM 调用。
- 清理只针对 `research_reports/assets/* 标签词云.png`，避免误删其他报告资源。

## 结果与复盘
- 词云图现在会显示周报统计时间范围，并保留所有非管理标签。
- `research_reports/assets` 只保留仍被周报 Markdown 引用的词云图文件。
- 真实 2026-04-17 周报已重建，golden 与 harness 已同步通过。

## 验证
- `./.venv/bin/python -m pytest -q`
- `./scripts/run_science_monitor.sh golden-eval`
- `./scripts/run_science_monitor.sh harness-check`
