# 计划：2026-04-09 UI 功能重构

## 目标
- 把 `config-ui` 从“通用控制台”继续收紧成面向周报、深度解读、人工中转的实际操作界面。
- 修正页面滚动、面板布局和当前“交互日志全局常驻”的问题。
- 为周报、深度解读、人工中转分别补充统计、最新结果和更直接的操作入口。

## 范围
- `config_ui.py`
- `config_ui_page.py`
- `config_ui_page_sections.py`
- `ui_assets/config_ui.css`
- `ui_assets/config_ui.js`
- `tests/test_config_ui.py`
- 如需要，补少量与人工中转请求生成相关的辅助函数

## 非目标
- 先不处理设置页重做
- 不改周报/深读底层业务生成逻辑
- 不提交、不推送 GitHub

## Progress
- [x] 梳理当前 UI 路由、可复用数据源和输出目录结构，确认周报/深读/人工中转各自需要的统计和最新结果。
- [x] 修复页面滚动与面板布局：右侧内容区恢复纵向滚动，子面板默认位置固定，不再根据宽度自动换位。
- [x] 重做总览页：运行统计、运行环境、LLM 状态、输出路径。
- [x] 重做周报页：周报生成参数、期刊缩写列表、周报最新结果。
- [x] 重做深度解读页：深度解读任务、可调 PDF 页数、深度解读数量统计、最新深读结果。
- [x] 重做人工中转页：最近活动、请求生成、响应文件解读、基于响应生成的最新结果。
- [x] 补测试并跑 `pytest` / `harness-check` / `maintenance-check` / `git diff --check`。

## 验证
- `./.venv/bin/python -m pytest tests/test_config_ui.py -q`
- `./scripts/run_science_monitor.sh harness-check`
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`
- `./.venv/bin/python -m pytest -q`
- `git diff --check`
