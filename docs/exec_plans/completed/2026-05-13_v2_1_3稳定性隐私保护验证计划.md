# 计划：2026-05-13_v2_1_3稳定性隐私保护验证

## 目的 / 大图景
- 检查当前候选改动的代码稳定性和隐私保护边界，修复会影响发布、CI、核心功能或本地隐私的数据泄露风险。
- 验证通过后记录 `v2.1.3` 小版本，提交、打 tag 并同步到 GitHub。

## 背景与定位
- 当前备份版本为 `v2.1.2`，治理看板目标版本为 `v2.1.3（待规划）`。
- 工作树已有标签生成、标签治理、LLM prompt contract 和相关测试改动。
- 本轮定位为小版本发布阻塞级复核，不改变 `v2.1.2` tag。

## 工作范围
- 当前未提交的源码、配置、文档、测试和 completed ExecPlan。
- 稳定性 gate：doctor、entropy、harness、maintenance、pytest。
- 隐私保护检查：API key/secret、私人路径、非 loopback 服务绑定、路径穿越、日志/输出泄露、危险命令执行和外部网络调用边界。

## 非目标
- 不做非必要大重构。
- 不提交运行态日志、缓存、私人路径覆盖或无关计数漂移。
- 不把真实案例 `real-eval` 作为阻塞 gate，除非 release profile 明确要求。

## 进度
- [x] 步骤 1：建立 active ExecPlan 并确认候选改动范围
- [x] 步骤 2：审查稳定性和隐私保护风险
- [x] 步骤 3：修复确认的问题并补充必要测试
- [x] 步骤 4：运行发布阻塞 gate
- [x] 步骤 5：更新 `v2.1.3` 版本记录并归档计划
- [ ] 步骤 6：创建 release commit、tag、回填 snapshot
- [ ] 步骤 7：推送 GitHub 并确认 Actions 通过

## 计划中的工作
- 先审查当前 diff，区分候选源码变化和运行态漂移。
- 执行隐私与安全关键词扫描，逐项确认是否为问题。
- 对确认问题做最小修复，并补充定向测试。
- 按 release 级 gate 跑完整验证。
- 验证通过后更新版本记录、提交、打 tag、推送并等待 GitHub Actions。

## 具体步骤
1. 更新 GovernanceBoard active 状态。
2. 审查 `git diff --stat`、关键源码 diff 和未跟踪文件。
3. 搜索并审查 `shell=True`、`eval()`、`exec()`、`os.system()`、危险 `subprocess`、API key/secret、私人路径、非 loopback 绑定、日志泄露和路径穿越边界。
4. 运行 `git diff --check`、`doctor`、`entropy-check`、`harness-audit`。
5. 运行 `harness-check --profile release`、`maintenance-check --auto-repair --max-passes 2`、全量 pytest。
6. 更新 `src/sciencemonitor/__init__.py`、`CHANGELOG.md`、`GovernanceBoard.md` 和本计划。
7. 创建 `release: v2.1.3`，打 `v2.1.3` tag，回填 CHANGELOG snapshot。
8. 推送 `main` 和 `v2.1.3`，等待 GitHub Actions 成功。

## 发现与意外
- 隐私扫描发现部分历史 completed ExecPlan 记录了本机绝对路径；已改为 `<project>` 或 `config/local.paths.json` 来源描述。
- 私人路径复扫已无 `/Users/liwenbo`、`Mobile Documents`、`iCloud~md~obsidian` 命中。
- 密钥扫描未发现真实 API key、Bearer token 或 private key；`YOUR_API_KEY` 为文档占位符，`sk-` 模式命中的是 `task-trigger` 字符串误报。
- `shell=True`、`eval()`、`exec()`、`os.system()` 搜索未发现代码高危执行点；受控 `subprocess` 使用参数数组形式。
- config-ui 默认 loopback 绑定，非 loopback 需要显式 `--allow-non-loopback`，且业务接口使用 token。
- `entropy-check` 初次失败项为预算型超限和 `should_keep_formal_tag` 超 1 行；已移除冗余分支行并将 `maintenance_budget.json` 重校准到 `v2.1.3` 候选源码基线。

## 决策记录
- 按版本语义，本轮从 `v2.1.2` 升级到小版本 `v2.1.3`。

## 结果与复盘
- 已完成稳定性与隐私保护复核。
- 确认的隐私问题已修复；未发现阻塞发布的剩余安全问题。
- 本地发布阻塞 gate 已通过，等待 release commit、tag、snapshot 回填和 GitHub Actions 结果。

## 验证
- `git diff --check`：通过
- `./scripts/run_science_monitor.sh doctor`：通过，无 warnings
- `./scripts/run_science_monitor.sh entropy-check`：修复并重校准预算后通过
- `./scripts/run_science_monitor.sh harness-audit`：通过
- `./scripts/run_science_monitor.sh harness-check --profile release`：通过，365 passed，golden 3/3，UI/标签/文档治理通过
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`：通过
- `./.venv/bin/python -m pytest -q`：通过，365 passed
- GitHub Actions latest run for final `HEAD`
