# 计划：2026-04-09 UI 细化第三轮

## 目标
- 修正总览页与全局布局在窄窗口下的宽度与溢出问题。
- 消除 config-ui 中误导性的环境 PATH 告警。
- 收紧导航栏与交互日志的信息展示，去掉冗余说明，补版本号与面板地址。
- 把人工中转最近活动从内部 request_id 改成面向用户的论文标签展示。

## 范围
- config-ui 页面结构与样式
- config-ui 页面数据来源与 doctor 调用口径
- chatgpt_web_manual 状态展示辅助信息
- 对应测试与变更记录

## 非目标
- 不改周报/深读业务逻辑
- 不做新的 UI 模块扩展
- 不提交、不推送 GitHub

## Progress
- [x] 调整布局：移除主内容最大宽度限制，保证右侧区域随窗口伸缩；保留左侧固定栏和可拖拽宽度。
- [x] 调整语义：把顶部和侧栏冗余说明去掉，品牌区增加当前版本号，侧栏补面板地址。
- [x] 修正环境状态：UI 页面使用更适合浏览器控制面的 doctor 口径，避免 PATH 假阳性；文案统一为“环境警告”。
- [x] 优化人工中转活动展示：提供“【状态】作者（年份）- 期刊缩写 - 题目”形式的显示标签，隐藏内部 request_id。
- [x] 补测试并跑 config-ui / harness / maintenance / 全量 pytest。

## 结果
- 右侧主内容不再受固定最大宽度约束，随浏览器窗口伸缩。
- UI 页面读取 `doctor` 时改为适合控制面的口径，不再显示 PATH 假阳性。
- 左侧品牌区显示当前版本号，状态区补充面板地址。
- 人工中转最近活动不再显示内部 `request_id`，改为面向用户的论文标签。
- `ExecPlan` 规范补回了 `Progress` 勾选清单要求，并同步修正了最近几份 UI 计划。

## 验证结果
- `./.venv/bin/python -m pytest tests/test_config_ui.py tests/test_chatgpt_web_manual.py -q` -> `10 passed`
- `./scripts/run_science_monitor.sh doctor --consistency-only` -> 通过
- `./scripts/run_science_monitor.sh harness-check` -> 通过
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2` -> 通过
- `./.venv/bin/python -m pytest -q` -> `133 passed`
- `git diff --check` -> 通过

## 验证
- `./.venv/bin/python -m pytest tests/test_config_ui.py tests/test_chatgpt_web_manual.py -q`
- `./scripts/run_science_monitor.sh harness-check`
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`
- `./.venv/bin/python -m pytest -q`
- `git diff --check`
