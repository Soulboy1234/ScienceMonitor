# 计划：2026-05-06_行星环境Tag统一

## 目的 / 大图景
- 统一 `行星空间环境` 与 `行星际环境` 两套相近 tag，避免正式词表、pending 和自动输出里继续分裂。
- 保留更符合当前文献语境的 `对象/行星际环境` 作为 canonical；`对象/行星空间环境` 仅作为旧输出别名归并。

## 背景与定位
- `formal_tags.md` 中存在正式 `对象/行星空间环境`。
- pending 和自动输出中存在 `对象/行星际环境/IMF构型`、`对象/行星际环境/日冕物质抛射CME` 等同源变体。
- 用户希望两套“行星空间环境 / 行星际环境”写法统一。

## 工作范围
- 正式 tag 人工入口与机器词表：`config/tag/formal_tags.md`、`config/focus_tags.json`
- tag 归一规则：`src/sciencemonitor/tag_governance.py`
- 测试：`tests/test_tag_review.py`
- 自动输出与 pending 刷新：`auto/article_summaries`、`auto/deep_reads`、`research_reports`、`config/pending_tags.json`、`config/tag/pending_tags.md`

## 非目标
- 不修改 `manual/` 手工笔记正文。
- 不改变研究方向 topic 名称和索引页标题中的自然语言描述。
- 不自动转正 pending 里的其他候选。

## 进度
- [x] 建立计划。
- [x] 更新正式 tag 与归一规则。
- [x] 重写自动输出并刷新 pending。
- [x] 运行测试和 harness。
- [x] 归档计划。

## 计划中的工作
- 将正式 tag 从 `对象/行星空间环境` 改为 `对象/行星际环境`。
- 为 `对象/行星际环境` 保留 `行星空间环境` 相关别名，兼容旧输出和未来 LLM 写法。
- 添加确定性映射：`IMF构型` 归并到 `指数/IMF`，`日冕物质抛射CME` 归并到 `事件/磁暴/CME`。
- 运行自动输出整治，清理 article summaries、deep reads、research reports 和 pending。

## 具体步骤
1. 修改 `formal_tags.md` 并同步 `focus_tags.json`。
2. 扩展 `tag_governance.py` 的旧输出别名和 pending 直接归并规则。
3. 增加 tag review 单元测试。
4. 执行 `tag-candidates --reconcile-output --limit 0`。
5. 检查残留、运行测试和 harness。

## 发现与意外
- 周报中有 `行星际环境/IMF构型` 的纯文本派生展示，不属于 `#tag` 行，因此输出重写命令不会自动改；已同步修正最新周报和对应日期周报中的派生显示。

## 决策记录
- canonical 使用 `对象/行星际环境`。
- `对象/行星空间环境`、`对象/行星空间环境/IMF构型` 归并到新 canonical 或更具体正式 tag。
- `对象/行星际环境/IMF构型` 归并到 `指数/IMF`；`对象/行星际环境/日冕物质抛射CME` 归并到 `事件/磁暴/CME`。

## 结果与复盘
- `config/tag/formal_tags.md` 现在只保留 `对象/行星际环境`，不再把 `对象/行星空间环境` 作为正式 tag。
- `config/focus_tags.json` 中 `对象/行星际环境` 保留 `对象/行星空间环境`、`行星空间环境`、`行星际环境` 作为别名。
- 自动输出整治扫描 64 个 Markdown 文件，修改 6 个文件；pending 从 54 个候选降到 51 个候选。
- 已有文章总结中的 `对象/行星际环境/IMF构型` 已重写为 `指数/IMF`，`对象/行星际环境/日冕物质抛射CME` 已重写为 `事件/磁暴/CME`。

## 验证
- `./.venv/bin/python -m pytest tests/test_tag_review.py tests/test_tag_candidates.py -q`：54 passed。
- `./.venv/bin/python -m pytest -q`：307 passed。
- `git diff --check`：通过。
- 首次 `./scripts/run_science_monitor.sh harness-check` 功能项均通过，失败点仅为本计划缺少模板必需标题；补齐并归档后重跑。
