# 计划：2026-04-14_等离子体对流标签归并

## 目的 / 大图景
- 把 `pending` 中的 `等离子体对流` 按用户刚确认的规则拆分处理。
- 对地球相关文章，改写为 formal 标签 `对象/极区/等离子体对流`。
- 刷新 `pending`，让剩余的 `等离子体对流` 只代表当前 formal 尚未覆盖的非地球语境。

## 背景与定位
- 当前 `pending` 中 `等离子体对流` 对应 11 篇 `auto/article_summaries`。
- 用户已明确给出归并规则：如果是地球的，可以归类到 `极区/等离子体对流`。

## 工作范围
- `auto/article_summaries` 中使用 `#等离子体对流` 的真实输出文件
- `config/pending_tags.json`
- `config/tag/pending_tags.md`

## 非目标
- 本轮不改 `manual` 输出。
- 本轮不扩展新的 formal taxonomy，只使用现有 `对象/极区/等离子体对流`。

## 进度
- [x] 步骤 1：识别 11 篇中哪些是地球语境。
- [x] 步骤 2：把地球语境文件改写为 `对象/极区/等离子体对流`。
- [x] 步骤 3：刷新 pending 并复查剩余文件。
- [x] 步骤 4：验证并归档计划。

## 结果与复盘
- 已把 6 篇地球语境文章中的 `#等离子体对流` 改写为 `#对象/极区/等离子体对流`。
- 当前保留 `#等离子体对流` 的文件还剩 5 篇，代表暂不并入 formal 的非地球或非明确极区语境。
- 刷新后，`pending_tags.md` 中 `等离子体对流` 的计数已从 11 降到 5。

## 验证
- `rg '#等离子体对流|#对象/极区/等离子体对流' ...`
- `PYTHONPATH=src ./.venv/bin/python - <<'PY' ... collect_output_tag_usage ...`
- `./scripts/run_science_monitor.sh harness-check`
