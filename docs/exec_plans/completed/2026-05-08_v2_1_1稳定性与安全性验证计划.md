# 计划：2026-05-08_v2_1_1稳定性与安全性验证

## 目的 / 大图景
- 将当前未提交改动作为 `v2.1.1` 小版本候选进行发布阻塞级稳定性与安全性验证。
- 发现会影响 CI、release gate、安全边界或核心功能的问题时做最小修复。
- 全部阻塞项通过后记录 `v2.1.1`，提交、打 tag 并推送 GitHub。

## 背景与定位
- 当前备份版本是 `v2.1.0`，治理看板目标版本为 `v2.1.1（待规划）`。
- 当前工作树已有大量未提交改动，涉及 harness、maintenance、CI、资源预检、测试隔离、配置 UI、HTTP/LLM 边界和文档治理。
- 本轮真实案例 `real-eval` 不作为发布阻塞 gate，只在结果中记录为非阻塞后续验证项。

## 工作范围
- `src/` 中稳定性、安全边界、harness、maintenance、资源预检、测试运行、配置 UI、HTTP/LLM 和路径相关代码。
- `tests/` 中对应单测与 gate 覆盖。
- `.github/workflows/ci.yml`、`scripts/`、`config/`、`docs/`、`CHANGELOG.md`、`GovernanceBoard.md` 和版本常量。

## 非目标
- 不做非阻塞大重构。
- 不改动 `v2.1.0` tag。
- 不把真实案例 fixture 作为本轮发布阻塞项。
- 不提交运行态日志、缓存、私人路径覆盖或无关机器资产计数漂移。

## 进度
- [x] 步骤 1：建立 active ExecPlan 并确认当前改动范围
- [x] 步骤 2：执行安全审查并修复发布阻塞问题
- [x] 步骤 3：按 smoke/default/output/ui/release 顺序执行 gate 并修复阻塞问题
- [x] 步骤 4：执行维护循环和全量测试
- [ ] 步骤 5：记录 `v2.1.1` 版本、提交、tag、推送并确认 GitHub Actions 通过
- [x] 步骤 6：归档本计划

## 计划中的工作
- 先做静态安全搜索和当前 diff 审查，确认是否存在阻塞风险。
- 从轻量 gate 到 release gate 逐步运行，失败即做最小修复并重跑相关项。
- 最后按 release checklist 记录版本和验证结果。

## 具体步骤
1. 新建本计划，更新治理看板 active 状态。
2. 搜索 `shell=True`、`eval()`、`exec()`、`os.system()`、危险 `subprocess`、非 loopback 绑定、路径穿越、输出删除开关和硬编码密钥。
3. 运行 `git diff --check`、`doctor`、`entropy-check`、`harness-audit`。
4. 运行 `harness-check --profile smoke/default/output/ui/release`。
5. 运行 `maintenance-check --auto-repair --max-passes 2` 和全量 `pytest -q`。
6. 更新版本文件、CHANGELOG 和 GovernanceBoard。
7. 创建 release commit、`v2.1.1` tag、回填 snapshot commit、推送 main 和 tag。
8. 等待最终 GitHub Actions run 成功。

## 发现与意外
- 安全搜索未发现 `shell=True`、`eval()`、`exec()`、`os.system()`。
- 受控 `subprocess` 调用均使用参数数组，集中在 launcher、PDF 抽取、git status、pytest runner 和 Codex provider。
- config-ui 当前候选实现已包含 loopback 默认绑定、会话 token、`/healthz` 探活、文件访问白名单和上传大小限制。

## 已通过的验证
- `git diff --check`：通过。
- `./scripts/run_science_monitor.sh doctor`：通过，无 warnings。
- `./scripts/run_science_monitor.sh entropy-check`：通过。
- `./scripts/run_science_monitor.sh harness-audit`：通过。
- `./scripts/run_science_monitor.sh harness-check --profile smoke`：通过，22 passed。
- `./scripts/run_science_monitor.sh harness-check --profile default`：通过，342 passed，golden 3/3。
- `./scripts/run_science_monitor.sh harness-check --profile output`：通过，扫描输出文件 103 个。
- `./scripts/run_science_monitor.sh harness-check --profile ui`：通过，UI 单测 39 passed，结构/功能/视觉均 ok。
- `./scripts/run_science_monitor.sh harness-check --profile release`：通过，342 passed，输出扫描 103 个，golden 3/3。
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`：通过，未应用自动修补。
- `./.venv/bin/python -m pytest -q`：通过，342 passed。

## 决策记录
- 修复边界采用“发布阻塞优先”。
- `real-eval` 不作为本轮阻塞项。

## 结果与复盘
- 本轮阻塞级稳定性与安全性验证均通过。
- 未把真实案例 `real-eval` 作为阻塞 gate；后续如需验证真实案例输出漂移，应单独运行 `harness-check --profile release --include-real-eval`。
- `pending_tags.json` 的大 diff 属于进入本轮前已有的候选治理资产变化，不是本轮 gate 运行产生的计数漂移。

## 验证
- `git diff --check`
- `./scripts/run_science_monitor.sh doctor`
- `./scripts/run_science_monitor.sh entropy-check`
- `./scripts/run_science_monitor.sh harness-audit`
- `./scripts/run_science_monitor.sh harness-check --profile smoke`
- `./scripts/run_science_monitor.sh harness-check --profile default`
- `./scripts/run_science_monitor.sh harness-check --profile output`
- `./scripts/run_science_monitor.sh harness-check --profile ui`
- `./scripts/run_science_monitor.sh harness-check --profile release`
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`
- `./.venv/bin/python -m pytest -q`
- GitHub Actions latest run for final `HEAD`
