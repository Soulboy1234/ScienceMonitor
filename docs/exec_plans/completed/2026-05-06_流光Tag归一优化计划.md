# 计划：2026-05-06_流光Tag归一优化

## 目的 / 大图景
- 将 `流光 / streamer(s)` 相关 tag 统一归到 `对象/极区/极光`，避免输出用户不熟悉且过细的 `对象/极区/极光/流光`。
- 保留其他有价值的四级标签，不做全局层级上限。

## 背景与定位
- 用户暂停了上一版“全部限制到三级”的方案，并指出部分四级标签仍然重要。
- 本轮只针对 `对象/极区/极光/流光` 及其 `streamer(s)` 别名做专门归一。

## 工作范围
- `src/sciencemonitor/tags.py`
- `src/sciencemonitor/tag_review.py`
- `tests/test_tag_review.py`
- 如有必要，更新 tag 参考或计划记录。

## 非目标
- 不删除正式标签库中历史已有的四级节点。
- 不限制所有 tag 到三级。
- 不重写所有既有输出正文；如 harness 要求，才通过既有 tag 审计/重写链路修正输出 tag 行。

## 进度
- [x] 建立计划。
- [x] 定位 tag 归一出口和深读 tag review 短语别名入口。
- [x] 实现流光专门折叠规则并补测试。
- [x] 运行相关测试与 harness gate。
- [x] 归档计划。

## 计划中的工作
- 撤销刚才未完成的全局三级折叠尝试。
- 在 `TagTaxonomy.normalize_tag()` 中把正式 `对象/极区/极光/流光` 重定向到 `对象/极区/极光`。
- 在深读 phrase alias 中把 `流光/streamer(s)` 映射到 `对象/极区/极光`。
- 补测试确认 `对象/热层/风场/垂直风` 这类四级标签仍可保留。
- 运行 targeted test、全量测试、`git diff --check` 和 `harness-check`。

## 具体步骤
1. 阅读 `tags.py` 的 normalize 流程和现有 tag review 测试。
2. 增加流光专门归一规则。
3. 更新或新增 tag review 测试。
4. 运行验证命令。

## 发现与意外
- 先前的全局三级折叠方案已撤销；`_inference_alias_pattern()` 属于此前为 FPI/ICON 边界修复引入的逻辑，保留不动。

## 决策记录
- 采用专门映射，不修改全局 tag 深度策略。

## 结果与复盘
- `对象/极区/极光/流光` 及 `流光 / streamer(s)` 相关输入现在统一归一到 `对象/极区/极光`。
- 没有引入全局 tag 层级上限；`对象/热层/风场/垂直风` 等有价值的四级标签仍可保留。
- 最新 Donaldson 深读输出的 tag 行已通过深读 tag 审计链路重写，移除了 `#对象/极区/极光/流光`，保留为 `#对象/极区/极光`。

## 验证
- `./.venv/bin/python -m pytest tests/test_tag_review.py tests/test_deep_reads.py tests/test_llm.py -q`：104 passed。
- `./.venv/bin/python -m pytest -q`：304 passed。
- `git diff --check`：通过。
- `./scripts/run_science_monitor.sh harness-check`：overall=ok。
