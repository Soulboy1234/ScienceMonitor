# 计划：2026-04-17_auto标签清理计划

## 目的 / 大图景
- 清理 `auto` 输出中误打的 `应用/卫星影响` 标签，让现有输出与最新标签审核规则一致。
- 这轮只做标签重审与治理收口，不调整周报正文、不重写文章总结内容。

## 背景与定位
- 标签审核规则已经收紧：只有正文主轴明确讨论空间环境或空间天气如何影响卫星运行时，才保留 `应用/卫星影响`。
- 当前 `auto` 输出仍保留旧规则遗留标签，`run_tag_output_review` 已识别出 15 个待修文件。
- `pending` 需要在重审后同步刷新，保持与真实输出一致。

## 工作范围
- `auto/article_summaries` 中受影响的输出文件
- `config/tag/pending_tags.md`
- `config/pending_tags.json`
- 本计划文件

## 非目标
- 不改 `manual` 输出
- 不改周报正文
- 不调整其他标签规则

## 进度
- [x] 步骤 1：确认现有标签审计结果和测试状态
- [x] 步骤 2：执行 `auto` 标签重审并刷新 pending
- [x] 步骤 3：运行 harness 验证并归档计划

## 计划中的工作
- 先记录当前待修文件和范围，避免无计划修改。
- 再运行标签重审，让 `auto` 输出落到新的审核规则。
- 最后跑审计与 harness，确认 `pending` 和真实输出重新一致。

## 具体步骤
1. 根据 `run_tag_output_review` 的结果确认当前需要清理的 `auto` 文件。
2. 运行 `reconcile_auto_output_tags_with_review`，只修改标签行并刷新 pending。
3. 复跑标签审计、测试和 `harness-check`。
4. 回填计划结果后移动到 `completed/`。

## 发现与意外
- 本轮真实输出中共有 15 个 `auto` 文件误打了 `应用/卫星影响`。
- 误打主要来自旧规则把卫星、轨道、阻力等背景性描述直接当成“卫星影响”应用标签。
- 新审核规则生效后，`run_tag_output_review` 已归零，说明 `auto` 与当前标签审核器重新一致。

## 决策记录
- 这轮只重审真实输出标签，不再修改标签规则本身。
- `应用/卫星影响` 的清理以最新审核器为准，不做人工逐篇例外处理。

## 结果与复盘
- 已完成 `auto/article_summaries` 的一次真实标签重审，共扫描 32 个文件，改写 15 个文件。
- 本轮只移除了误打的 `应用/卫星影响`，未改动正文、摘要和其他标签规则。
- `pending` 已随重审刷新；`harness-check` 通过，标签治理与输出审计均为 `ok`。

## 验证
- `./.venv/bin/python -m pytest tests/test_article_summaries.py tests/test_tag_review.py -q`
- `./scripts/run_science_monitor.sh harness-check`
