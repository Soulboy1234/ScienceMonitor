# 计划：2026-04-28_UI当前单篇期刊显示

## 目的 / 大图景
- 在周报单篇总结阶段显示当前文章所属期刊，便于判断当前处理进度。
- 保持当前正在运行的周报任务不中断。

## 背景与定位
- UI 当前只显示 `summary_current_title`，缺少期刊信息。
- 当前任务正在运行，不能重启 UI 服务或停止 worker。

## 工作范围
- 单篇总结进度事件字段。
- UI 状态合并字段。
- 服务端和前端状态卡片显示。
- 当前运行态的无中断补丁。

## 非目标
- 不停止当前周报任务。
- 不修改周报正文生成逻辑。

## 进度
- [x] 步骤 1：进度事件增加期刊字段
- [x] 步骤 2：UI 显示当前期刊与题目
- [x] 步骤 3：补测试并更新当前运行态

## 计划中的工作
- 新增 `summary_current_journal`。
- “当前单篇”显示为 `期刊 · 题目`。
- 对当前运行任务，从数据库按标题查期刊写回状态文件。

## 具体步骤
1. 修改 `article_summary_progress.py`、`article_summaries.py` 和 `config_ui_report_jobs.py`。
2. 修改 `config_ui_result_cards.py` 和 `ui_assets/config_ui.js`。
3. 更新 `test_config_ui.py`。
4. 不重启 UI，仅更新当前 `config_ui_state.json`。

## 发现与意外
- 当前正在运行的 worker 仍是旧代码，不会主动发出 `summary_current_journal`；因此对当前运行态做了一次无中断补丁，把期刊前缀写入 `summary_current_title`。
- 新代码已处理“期刊已在标题前缀中”的情况，避免页面刷新或下轮任务中重复显示期刊。

## 决策记录
- 期刊是当前单篇的上下文，不再复用 `current_source` 字段。

## 结果与复盘
- 新增 `summary_current_journal` 进度字段。
- UI 当前单篇显示为 `期刊 · 题目`。
- 当前正在运行的任务已按数据库标题查到期刊并更新状态，不停止周报进程。

## 验证
- `./.venv/bin/python -m pytest tests/test_config_ui.py -q`：通过，21 passed。
- `git diff --check`：通过。
