# 计划：2026-05-08_批量深读Ollama结构化恢复加固

## 目的 / 大图景
- 让 Ollama 批量深度解读在 PDF 文本已成功抽取时，不再因为可恢复的 JSON 格式坏块直接失败。
- 本轮只加固 Ollama 深读结构化输出恢复、重试和运行验证，不改变 Codex 深读正文生成逻辑。

## 背景与定位
- 2026-05-08 UI 批量深读任务中，4 篇 PDF 的文本来源缓存均已成功生成，失败点集中在 Ollama evidence/final JSON 解析。
- 失败样式包括 evidence 字段名局部截断或缺引号、final JSON 被 code fence 包裹、字符串尾部多余引号、根对象中过早出现额外 `}`，以及严格重试后缺少 `final_conclusion`。
- 另外，当前 config-ui 进程可能仍在运行旧版内存代码，需要在修复后重启服务以确保 UI 使用最新恢复逻辑。

## 工作范围
- `src/sciencemonitor/llm.py`
- `tests/test_llm.py`
- `docs/exec_plans/active/` 与完成后的 `docs/exec_plans/completed/`
- 必要时更新 `docs/exec_plans/GovernanceBoard.md`

## 非目标
- 不把 Ollama 失败自动切换到 Codex。
- 不降低深度解读内容字段要求。
- 不重写已经成功生成的深度解读正文，除非真实重跑验证自然生成新输出。
- 不调整通用 PDF 抽取、标签治理或 Codex prompt。

## 进度
- [x] 核对本次 4 篇失败的阶段与日志样式
- [x] 加固 Ollama 深读本地 JSON 修复
- [x] 让严格重试输出也能经过本地修复
- [x] 补充单元测试覆盖本次失败样式
- [x] 重启 config-ui 并重跑失败批量样例
- [x] 运行 gate 并归档计划

## 计划中的工作
- 先把 evidence/final 的真实坏 JSON 样式归纳为确定性修复规则。
- 在 Ollama-only 深读恢复层处理字段别名、缺失 required 默认值、额外根对象闭合和字符串尾部多余引号。
- 严格重试失败时也先尝试本地修复，只有不可恢复才向上抛错。
- 用单元测试固定本次 4 篇暴露的错误形态。
- 修复后重启 UI 服务并重跑最近上传文件夹，确认不再出现同类失败。

## 具体步骤
1. 阅读 `log/llm_tmp` 中本次批量失败的 invalid output，确认每篇失败阶段。
2. 修改 `llm.py` 的 Ollama 深读修复函数和重试分支。
3. 在 `tests/test_llm.py` 增加 evidence truncated key、final missing conclusion、extra root brace、retry repair 的回归测试。
4. 运行 `./.venv/bin/python -m pytest tests/test_llm.py -q`。
5. 重启 `config-ui`，重跑 `tmp/ui_folder_uploads/20260508-182056`。
6. 运行相关 gate，记录结果并归档。

## 发现与意外
- 4 篇失败的 PDF 文本缓存均已成功生成，失败不是 PDF 读取问题。
- Zhang evidence 原始输出存在 `_open_questions"` 与 `ers_findings"` 这类字段名局部截断。
- Q-Sat final 原始输出在 `reproducibility` 字符串末尾多了一个双引号。
- SYM-H final 原始输出在 `reproducibility` 后提前关闭了根对象，后续字段仍然存在。
- Bussy、Q-Sat、SYM-H 的严格重试输出均接近完整，但缺少 `final_conclusion`，旧逻辑没有对 retry 输出再做本地修复。
- 重跑 `tmp/ui_folder_uploads/20260508-182056` 时，4 篇 PDF 全部生成，失败 0；其中 Zhang evidence、Q-Sat/Bussy/SYM-H final 均出现过 invalid-output 文件，但已被新恢复层接管并写出 Markdown。

## 决策记录
- 对缺少 `final_conclusion` 的可恢复输出，使用 `one_sentence_overview` 作为保守兜底，并在 `needs_manual_review` 记录本地补齐字段，避免静默伪造结论。
- 只在 `deep_read_analysis` 与 `ollama_deep_read_evidence_v2` schema 下启用深读专项修复，避免影响单篇总结、周报和 Codex 路径。

## 结果与复盘
- 已确认本次失败不是 PDF 抽取问题，而是 Ollama 深读结构化 JSON 的可恢复坏块。
- `llm.py` 现在会在 Ollama 深读 evidence/final/revision 路径中修复字段名截断、markdown/fence 残留、数组残片、尾部多余引号、LaTeX 反斜杠、`reproducibility` 后多余根对象闭合，以及 retry 输出缺少 `final_conclusion` 的情况。
- 严格重试输出失败后也会再尝试本地修复，避免“初稿可恢复但 retry 直接抛错”。
- 最近上传文件夹 4 篇真实 PDF 已重跑成功，`Generated 4; failed 0`。
- config-ui 已重启为后台进程，状态文件刷新到 2026-05-08 19:00:40。

## 验证
- `./.venv/bin/python -m pytest tests/test_llm.py -q`：45 passed
- `./.venv/bin/python -m pytest tests/test_deep_reads.py tests/test_config_ui.py -q`：75 passed
- `./scripts/run_science_monitor.sh deep-read-folder --folder /Users/liwenbo/Documents/codex/ScienceMonitor/tmp/ui_folder_uploads/20260508-182056`：Generated 4; failed 0
- `./.venv/bin/python -m pytest -q`：354 passed
- `git diff --check`：通过
- `./scripts/run_science_monitor.sh harness-check`：overall=ok
