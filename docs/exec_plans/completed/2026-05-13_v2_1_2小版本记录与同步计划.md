# 计划：2026-05-13_v2_1_2小版本记录与同步

## 目的 / 大图景
- 将当前已完成的深度解读、Ollama 结构化恢复、PDF 抽取和标签治理改动记录为 `v2.1.2` 小版本。
- 在同步 GitHub 前执行项目发布 gate，确认当前候选状态可作为稳定备份点。

## 背景与定位
- 当前备份版本为 `v2.1.1`，治理看板目标版本为 `v2.1.2（待规划）`。
- 工作树已有多文件改动，包含源码、配置、golden fixture、测试和 completed ExecPlan。
- 本轮定位为小版本记录，不改变 `v2.1.1` tag。

## 工作范围
- 深度解读、Ollama 结构化输出恢复、网页/PDF 抽取、文章索引和标签治理相关源码。
- `config/` 标签资产、分析配置和同步配置。
- `tests/`、golden fixture、CHANGELOG、GovernanceBoard 和本轮 ExecPlan。

## 非目标
- 不做新的功能重构。
- 不把真实案例 `real-eval` 作为阻塞 gate，除非发布 gate 明确要求。
- 不提交运行态日志、缓存、私人路径覆盖或无关临时文件。

## 进度
- [x] 步骤 1：确认当前改动范围并建立 active ExecPlan
- [x] 步骤 2：运行发布前检查并修复阻塞问题
- [x] 步骤 3：更新 `v2.1.2` 版本记录
- [x] 步骤 4：创建 release commit、tag、回填 snapshot
- [ ] 步骤 5：推送 GitHub 并确认 Actions 通过
- [x] 步骤 6：归档本计划

## 计划中的工作
- 先运行轻量检查确认没有格式、配置和熵问题。
- 再运行 release harness、维护循环和全量 pytest。
- 验证通过后更新版本记录、提交、打 tag 并推送。

## 具体步骤
1. 更新 GovernanceBoard active 状态。
2. 运行 `git diff --check`、`doctor`、`entropy-check`、`harness-audit`。
3. 运行 `harness-check --profile release`、`maintenance-check --auto-repair --max-passes 2`、全量 pytest。
4. 更新 `src/sciencemonitor/__init__.py`、`CHANGELOG.md`、`GovernanceBoard.md`。
5. 创建 `release: v2.1.2`，打 `v2.1.2` tag，回填 CHANGELOG snapshot commit。
6. 推送 `main` 和 `v2.1.2`，等待 GitHub Actions 成功。

## 发现与意外
- `entropy-check` 初次失败项为预算型超限，无未使用导入或 import cycle；已将 `maintenance_budget.json` 重校准到 `v2.1.2` 候选源码基线。
- `real-eval` 未纳入本轮阻塞 gate，按计划保留为后续真实案例验证项。

## 已通过的验证
- `git diff --check`：通过。
- `./scripts/run_science_monitor.sh doctor`：通过，无 warnings。
- `./scripts/run_science_monitor.sh harness-audit`：通过。
- `./scripts/run_science_monitor.sh entropy-check`：重校准维护预算后通过。
- `./scripts/run_science_monitor.sh harness-check --profile release`：通过，362 个 pytest、3 个 golden case 和 UI/标签/文档治理检查均通过。
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`：通过，1 pass，无需自动修复。
- `./.venv/bin/python -m pytest -q`：通过，362 passed。

## 决策记录
- 按版本语义，本轮从 `v2.1.1` 升级到小版本 `v2.1.2`。

## 结果与复盘
- 本地发布阻塞 gate 已通过。
- `v2.1.2` release commit 和 tag 已创建，CHANGELOG snapshot 已回填。
- GitHub push 和 Actions 结果在推送后通过最终答复记录。

## 验证
- `git diff --check`
- `./scripts/run_science_monitor.sh doctor`
- `./scripts/run_science_monitor.sh entropy-check`
- `./scripts/run_science_monitor.sh harness-audit`
- `./scripts/run_science_monitor.sh harness-check --profile release`
- `./scripts/run_science_monitor.sh maintenance-check --auto-repair --max-passes 2`
- `./.venv/bin/python -m pytest -q`
- GitHub Actions latest run for final `HEAD`
